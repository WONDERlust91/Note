// 文档存放目录，影响 vitepress config 及 nav, sidebar 生成
export const srcDir = "posts";
// 需要自动读取并生成 nav 与 sidebar 的目录
export const directories = ["listening", "reading", "writing"];
// 首页按钮重定向跳转目录
export const redirectDir = directories[1];
